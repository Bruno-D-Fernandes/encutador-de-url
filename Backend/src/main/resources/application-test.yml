spring:
  application:
    name: encurtaUrl
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop